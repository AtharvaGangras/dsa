    // int n = arr.size();
    // vector<int> tmp(n-m-1,0);
    // for(int i = m+1;i<n;i++)
    // {
    //     //put the values in array in reverse order
    //     tmp[i-m-1] = arr[n-i+m];
    // }
    // //copy paste in old array
    // for(int i = m+1;i<n;i++)
    // {
    //     arr[i] = tmp[i-m-1];
    // }